# Web Framework
fastapi==0.115.5
uvicorn[standard]==0.32.1
python-multipart==0.0.12

# Database
sqlalchemy==2.0.36
pydantic-settings==2.6.1
psycopg2-binary>=2.9          # PostgreSQL driver (Supabase production)
PyJWT>=2.8.0                  # Supabase JWT verification

# PDF Processing
PyMuPDF==1.24.13
pdfplumber==0.11.4

# Embeddings
sentence-transformers==3.3.1
torch==2.5.1

# Vector Database
chromadb>=0.6.0

# LLM Providers
# 100% local (no API key needed):
ollama==0.4.4              # Local models via Ollama (ollama.com)
# httpx is unpinned so pip can satisfy ollama's httpx<0.28.0 requirement
httpx>=0.27.0,<0.28.0
# FREE cloud providers:
groq==0.13.0               # Free tier: console.groq.com
huggingface-hub==0.26.5    # Free tier: huggingface.co/settings/tokens
# Paid providers (optional):
openai==1.57.4
anthropic==0.40.0
google-generativeai>=0.8.3           # gemini-2.0-flash requires >=0.8.0

# Frontend
streamlit==1.40.2
requests==2.32.3

# Testing
pytest==8.3.4
pytest-asyncio==0.24.0
# httpx already pinned above - used by both ollama and httpx test client

# OCR (optional — needed for scanned PDFs)
# Requires system package: apt-get install tesseract-ocr
# Local: pip install pytesseract Pillow
pytesseract>=0.3.10
Pillow>=10.0.0

# Utilities
python-dotenv==1.0.1
